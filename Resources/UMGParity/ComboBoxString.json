{
	"umgClass": "UComboBoxString",
	"umgHeader": "Components/ComboBoxString.h",
	"dreamClass": "UDreamDropdown",
	"members": [
		{"umg": "DefaultOptions", "kind": "property", "status": "map", "dream": "Options", "note": "One array, not two. UMG keeps DefaultOptions beside a runtime list because its combo box rebuilds from the Slate widget's own copy; here the rows are built from Options on every push, so the authored array IS the live one."},
		{"umg": "SelectedOption", "kind": "property", "status": "map", "dream": "SelectedIndex", "note": "The index is the authored truth and the option text follows from it (GetSelectedOption / SetSelectedOption). Two authored spellings of one selection is how an array edit leaves them disagreeing."},
		{"umg": "WidgetStyle", "kind": "property", "status": "map", "dream": "Style", "note": "FDreamDropdownStyle: the face's five state colours and brush, the list's background, the rows, the check mark and the caption."},
		{"umg": "ItemStyle", "kind": "property", "status": "map", "dream": "FDreamDropdownStyle::ItemBrush", "note": "A row's look is part of the dropdown's own style rather than a separate table-row style: ItemBrush, ItemHeight, ItemHovered, ItemFocused, ItemDisabled and CheckColor beside it."},
		{"umg": "ScrollBarStyle", "kind": "property", "status": "reject", "dream": "", "note": "There is no bar to style. This dropdown's open list is exactly MaxVisibleItems tall and scrolls by wheel and drag with no bar drawn -- a list capped at a row count is always the size it looks, which is what makes the bar unnecessary rather than missing. The family's bar style is FDreamScrollBarStyle and its control is Native.ScrollBar; a list that wants one is a Native.ScrollBox, not a combo box."},
		{"umg": "ContentPadding", "kind": "property", "status": "map", "dream": "FDreamDropdownStyle::ContentPadding", "note": "In the style, and its default is what the control has always arranged down to the number (ten left, twenty-four right to clear the arrow). Stating it makes a wider face, or a face with no arrow, something a project sheet can answer."},
		{"umg": "MaxListHeight", "kind": "property", "status": "map", "dream": "MaxVisibleItems", "note": "A row COUNT rather than a pixel height, because that is how a designer thinks about a dropdown; the open list is exactly count-times-ItemHeight tall and the rest scroll."},
		{"umg": "HasDownArrow", "kind": "property", "status": "map", "dream": "bHasDownArrow", "note": "Named with this codebase's bool prefix."},
		{"umg": "EnableGamepadNavigationMode", "kind": "property", "status": "reject", "dream": "", "note": "A switch between two behaviours, of which only one exists here -- and it is the one the flag turns ON. An open list's rows are ordinary selectables: a direction moves focus and commits nothing until the row is activated, which is exactly UMG's gamepad navigation mode. The OFF state, where a direction on the CLOSED control changes the value without opening it, has no counterpart and should not gain one: a closed control that eats left and right is the trap UUISlider::RequiresControllerLock exists to keep a row of controls out of."},
		{"umg": "Font", "kind": "property", "status": "map", "dream": "FDreamDropdownStyle::FontSize", "note": "A size, not an FSlateFontInfo: the typeface is the text visual's own (UDreamText), and what a dropdown's style decides is how big its caption and rows are."},
		{"umg": "ForegroundColor", "kind": "property", "status": "map", "dream": "FDreamDropdownStyle::TextColor", "note": "The caption's and the rows' colour. FDreamUIStateFaces beside it carries a per-state foreground for a face that wants its caption to change with the pointer."},
		{"umg": "bIsFocusable", "kind": "property", "status": "map", "dream": "UDreamWidget::bIsFocusable", "note": "Every widget here carries it."},
		{"umg": "OnGenerateWidgetEvent", "kind": "property", "status": "map", "dream": "ItemTemplateClass", "note": "A CLASS rather than a per-row delegate, with OnItemGenerated fired for each row to fill it. Same bargain as UDreamListViewBase::RowTemplateClass: a class is something .dui and the designer can name, and a delegate is not."},
		{"umg": "OnSelectionChanged", "kind": "event", "status": "map", "dream": "OnSelectionChanged", "note": "Carries the index. UMG's second argument, ESelectInfo, has no counterpart: the behaviour raises one value-changed seam whatever moved the value, so 'who changed it' is not a question this control can answer honestly."},
		{"umg": "OnOpening", "kind": "event", "status": "adopt", "dream": "", "note": "Fired from the behaviour's Show, before the rows are placed -- so options written from a handler are the ones the player sees. OnClosed is the pair UMG has no name for."},
		{"umg": "AddOption", "kind": "function", "status": "adopt", "dream": "", "note": "FText rather than FString: an option is something a player reads, and this framework carries FText all the way."},
		{"umg": "RemoveOption", "kind": "function", "status": "adopt", "dream": "", "note": "Takes the index-matched icon with it."},
		{"umg": "FindOptionIndex", "kind": "function", "status": "adopt", "dream": "", "note": "Compares by meaning (FText::EqualTo), so a localized option matches across cultures."},
		{"umg": "GetOptionAtIndex", "kind": "function", "status": "adopt", "dream": "", "note": "Empty text for an index nobody offers."},
		{"umg": "ClearOptions", "kind": "function", "status": "adopt", "dream": "", "note": "Options and icons; the selection is ClearSelection's business, as in UMG."},
		{"umg": "ClearSelection", "kind": "function", "status": "adopt", "dream": "", "note": "Index -1, which is what this control already spells 'none'."},
		{"umg": "RefreshOptions", "kind": "function", "status": "adopt", "dream": "", "note": "For a caller who edited the array in place."},
		{"umg": "SetSelectedOption", "kind": "function", "status": "adopt", "dream": "", "note": "An option nobody offers changes nothing, rather than clearing the selection."},
		{"umg": "SetSelectedIndex", "kind": "function", "status": "adopt", "dream": "", "note": ""},
		{"umg": "GetSelectedOption", "kind": "function", "status": "adopt", "dream": "", "note": ""},
		{"umg": "GetSelectedIndex", "kind": "function", "status": "adopt", "dream": "", "note": ""},
		{"umg": "GetOptionCount", "kind": "function", "status": "adopt", "dream": "", "note": ""},
		{"umg": "IsOpen", "kind": "function", "status": "adopt", "dream": "", "note": "Mirrored from the behaviour's visibility seam, so a list closed by a click elsewhere is not still open here."},
		{"umg": "SetStyle", "kind": "function", "status": "adopt", "dream": "", "note": "Not a UFUNCTION on UComboBoxString (its Setter meta names it); added here so a look can be replaced at runtime."}
	]
}
