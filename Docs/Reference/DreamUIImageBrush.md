# FDreamUIImageBrush

<!-- Generated by the DreamGUIReferenceDocs commandlet from reflection. Edit the header, not this page. -->

Declared in `Public/Core/DreamUIImageBrush.h`.

Dream UIImage Brush

## Properties

| Name | Type | Category | In the details panel | From Blueprint | Description |
|---|---|---|---|---|---|
| `ResourceObject` | `TObjectPtr<UObject>` | ImageBrush | yes | read / write |  |
| `TintColor` | `FColor` | ImageBrush | yes | read / write |  |
| `DrawAs` | `EDreamUIImageBrushDrawType` | ImageBrush | yes | read / write |  |
| `ImageSize` | `FVector2f` | ImageBrush | yes | read / write |  |
| `Margin` | `FMargin` | ImageBrush | yes | read / write | Margin size from 0 to 1. |
| `UVRegion` | `FVector4f` | ImageBrush | yes | read / write | UV region for an image, xy for min and zw for max. |
| `PixelsPerUnitMultiplier` | `float` | ImageBrush | yes | read / write |  |
| `FillAmount` | `float` | ImageBrush | yes | read / write | Filled only: how much of the image is shown, 0 shows none of it and 1 all of it. |
| `FillMethod` | `EDreamUIImageBrushFillMethod` | ImageBrush | yes | read / write | Filled only: the shape of the cut. |
| `FillOrigin` | `uint8` | ImageBrush | yes | read / write | Filled only: which corner or edge a radial fill starts from. Held as a byte for the same reason UDreamSprite holds it as one: which enum it means depends on FillMethod (EDreamUISpriteFillOriginType_Radial90 / _Radial180 / _Radial360), and only one of them applies at a time. Ignored by the horizontal and vertical methods. |
| `FillDirectionFlip` | `bool` | ImageBrush | yes | read / write | Filled only: fill from the other end. |
| `CornerRadius` | `FVector4f` | ImageBrush | yes | read / write | RoundedBox only: corner radius in pixels, X top-left, Y top-right, Z bottom-right, W bottom-left -- clockwise from the top left, the order CSS and UDreamRectBlock both use. Each is clamped to half the shorter side, so a big enough radius gives a capsule rather than an error. |
| `CornerSegments` | `int32` | ImageBrush | yes | read / write | RoundedBox only: points along each corner arc. More is smoother and costs two triangles each. |

