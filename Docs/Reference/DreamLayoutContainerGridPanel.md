# UDreamLayoutContainerGridPanel

<!-- Generated by the DreamGUIReferenceDocs commandlet from reflection. Edit the header, not this page. -->

Inherits from [UDreamPanelLayoutBase](DreamPanelLayoutBase.md).

Declared in `Public/Core/Components/DreamPanelLayouts.h`.

UMG Grid Panel

## Properties

| Name | Type | Category | In the details panel | From Blueprint | Description |
|---|---|---|---|---|---|
| `Padding` | `FMargin` | GridPanel | yes | `SetPadding` |  |
| `Spacing` | `FVector2D` | GridPanel | yes | `SetSpacing` |  |
| `ColumnFill` | `TArray<float>` | GridPanel | yes | `SetColumnFill` |  |
| `RowFill` | `TArray<float>` | GridPanel | yes | `SetRowFill` |  |

## Functions

| Function | Kind | Description |
|---|---|---|
| `void ClearFill()` | callable | Drop every fill coefficient, so every track goes back to sizing itself from its children. |
| `void SetColumnFill(TArray<float> Value)` | callable | Set Column Fill |
| `void SetColumnFillAt(int32 InColumnIndex, float InCoefficient)` | callable | UMG's UGridPanel::SetColumnFill(Index, Coefficient), under a name that says it addresses one track. |
| `void SetPadding(FMargin Value)` | callable | Set Padding |
| `void SetRowFill(TArray<float> Value)` | callable | Set Row Fill |
| `void SetRowFillAt(int32 InRowIndex, float InCoefficient)` | callable | Set Row Fill At |
| `void SetSpacing(FVector2D Value)` | callable | Set Spacing |

## Compared with UMG

Every Blueprint-facing member of the UMG class, and where it went. *adopt*: same name, same meaning. *map*: here under another name or on another type. *reject*: deliberately absent, with the reason.

| UMG | Member | Status | Here | Note |
|---|---|---|---|---|
| `UGridPanel` | `ColumnFill` | adopt | `ColumnFill` |  |
| `UGridPanel` | `RowFill` | adopt | `RowFill` |  |
| `UGridPanel` | `AddChildToGrid` | map | `AddChild` | One slot class here means one verb; the row and column are set on the slot it returns. |
| `UGridPanel` | `SetColumnFill` | map | `SetColumnFillAt` | UMG's takes an index and a coefficient. The plain name is already the whole-array BlueprintSetter for ColumnFill and two reflected functions cannot share a name, so the per-track form carries the suffix. |
| `UGridPanel` | `SetRowFill` | map | `SetRowFillAt` | As SetColumnFill. |
| `UGridPanel` | `ClearFill` | adopt | `ClearFill` |  |

